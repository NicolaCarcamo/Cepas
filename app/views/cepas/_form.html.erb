<%= form_with(model: cepa) do |form| %>
  <% if cepa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cepa.errors.count, "error") %> prohibited this cepa from being saved:</h2>

      <ul>
        <% cepa.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
